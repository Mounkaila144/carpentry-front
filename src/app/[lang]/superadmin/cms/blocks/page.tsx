'use client'

import { BlocksList } from '@/modules/Cms/superadmin/components/BlocksList'

/**
 * Superadmin CMS Blocks Page
 * Displays the list of all CMS blocks
 */
export default function CmsBlocksPage() {
  return <BlocksList />
}
